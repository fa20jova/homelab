apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: dash
spec:
  type: LoadBalancer
  loadBalancerIP: 172.20.50.57
  selector:
    app.kubernetes.io/name: homepage
  ports:
    - protocol: TCP
      port: 8385
      targetPort: http
